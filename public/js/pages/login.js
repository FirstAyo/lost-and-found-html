/**
 * Phase 2 keeps authentication server-rendered for a simpler, more reliable flow.
 * This page script exists so future enhancements can add richer client-side UX.
 */
console.debug('login page ready');
